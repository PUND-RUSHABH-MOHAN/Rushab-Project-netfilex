# This is a placeholder - MediaConvert doesn't have Terraform resource management
# Jobs are created by Lambda #1 at runtime

output "mediaconvert_info" {
  value = "MediaConvert jobs are created dynamically by Lambda #1"
}
